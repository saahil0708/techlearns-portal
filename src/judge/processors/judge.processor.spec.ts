import { describe, expect, it } from 'vitest';

describe('JudgeProcessor (Disabled for now)', () => {
  it('should be skipped until BullMQ is re-enabled', () => {
    expect(true).toBe(true);
  });
});
