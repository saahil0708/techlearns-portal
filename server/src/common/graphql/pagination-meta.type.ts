import { Field, Int, ObjectType } from '@nestjs/graphql';

@ObjectType('PaginationMeta')
export class PaginationMeta {
  @Field(() => Int)
  total: number;

  @Field(() => Int)
  page: number;

  @Field(() => Int)
  limit: number;

  @Field(() => Int)
  totalPages: number;
}
