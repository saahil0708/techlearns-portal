import { isFunction } from '@nestjs/common/utils/shared.utils.js';
export function isPipe(value) {
    if (!value) {
        return false;
    }
    if (isFunction(value)) {
        return true;
    }
    return isFunction(value.transform);
}
