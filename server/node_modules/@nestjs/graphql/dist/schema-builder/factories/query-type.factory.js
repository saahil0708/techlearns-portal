import { __decorate, __metadata } from "tslib";
import { Injectable } from '@nestjs/common';
import { TypeMetadataStorage } from '../storages/type-metadata.storage.js';
import { RootTypeFactory } from './root-type.factory.js';
let QueryTypeFactory = class QueryTypeFactory {
    constructor(rootTypeFactory) {
        this.rootTypeFactory = rootTypeFactory;
    }
    create(typeRefs, options) {
        const objectTypeName = 'Query';
        const queriesMetadata = TypeMetadataStorage.getQueriesMetadata();
        return this.rootTypeFactory.create(typeRefs, queriesMetadata, objectTypeName, options);
    }
};
QueryTypeFactory = __decorate([
    Injectable(),
    __metadata("design:paramtypes", [RootTypeFactory])
], QueryTypeFactory);
export { QueryTypeFactory };
