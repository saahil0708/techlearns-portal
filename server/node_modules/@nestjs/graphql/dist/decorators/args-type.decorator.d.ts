import { RegisterInOption } from '../schema-builder/metadata/index.js';
/**
 * Interface defining options that can be passed to `@ArgsType()` decorator.
 *
 * @publicApi
 */
export interface ArgsTypeOptions {
    /**
     * NestJS module that this type belongs to.
     * When specified, this type will only be included in GraphQL schemas
     * that include this module via the `include` option.
     * @see RegisterInOption for details
     */
    registerIn?: RegisterInOption;
}
/**
 * Decorator that marks a class as a resolver arguments type.
 *
 * @publicApi
 */
export declare function ArgsType(): ClassDecorator;
/**
 * Decorator that marks a class as a resolver arguments type.
 *
 * @publicApi
 */
export declare function ArgsType(options: ArgsTypeOptions): ClassDecorator;
/**
 * Decorator that marks a class as a resolver arguments type.
 *
 * Note: arguments classes are flattened into the individual arguments of every
 * field they are used in, so the explicit name is only reflected in the type
 * metadata and does not appear in the generated schema.
 *
 * @publicApi
 */
export declare function ArgsType(name: string, options?: ArgsTypeOptions): ClassDecorator;
//# sourceMappingURL=args-type.decorator.d.ts.map